# import Subscription.price, Post, Price, Mutation.createPrice from "./generated/prisma.graphql"

type Query {
  feed: [Post!]!
}

type Mutation {
  makePrices: Response!
}

type Error {
  field: String!
  msg: String!
}

type Response {
  isSuccess: Boolean
  error: Error
}